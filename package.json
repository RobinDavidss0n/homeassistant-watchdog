{
  "name": "ha-watchdog",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "dev": "docker compose --profile dev up --build --watch --force-recreate --remove-orphans dev"
  },
  "dependencies": {
    "playwright": "^1.43.0",
    "undici": "^8.2.0",
    "winston": "^3.13.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.4.5"
  }
}
